CHASS KEIBA LAB Ver.9.9.26
Race Volatility Intelligence

- 過去の検証済み類似レースを類似度加重して波乱率・順当率を算出
- AI上位団子度、市場乖離、危険人気馬、穴馬密度、TIME接近度、展開不確実性をUpset Scoreへ反映
- AI突出、能力差、TIME差、人気整合、類似順当率をStability Scoreへ反映
- データ不足時は50%方向へ縮小し、指数とは別に信頼度を保存
- predictionSnapshotへ波乱指数と説明情報を固定保存
- レース上部と検証ダッシュボードへ波乱指数・較正指標を追加
- NAR通信、自動結果取得、D1 schema、AI・TIME・穴馬ロジックは変更なし

旧版記録: Ver.9.9.25
Automatic Post-Race Result Queue

- 予想保存時にraceId単位の結果待ちキューへ登録
- 発走予定+10分から5/5/10/15分間隔で再確認
- ブラウザ60秒scheduler（due時のみ通信）
- Cloudflare Cron 5分間隔、最大5Rを完全逐次処理
- 手動予想・手動結果・現在オッズを自動回収より優先
- Historical Collectorより自動結果回収を優先
- D1既存race_json/statusを利用（DROP/DELETE/new tableなし）
- predictionSnapshot・AI・TIME・穴馬ロジック変更なし

旧版記録: Ver.9.9.22
Stable Transport Rebase

・通常結果取得を /api/nar/sync へ統一
・Ver.9.9.8型の単純通信構造へ部分的に復帰
・sync-minimal / result-diagnostic は通常失敗時のみ使用
・同時実行防止を単純な処理中ロックへ整理
・D1、検証、UI、Snapshot、予想・穴馬ロジックは維持

旧版履歴：
CHASS KEIBA LAB Ver.9.9.21

Result Fetch Single-Flight / Safari Abort Isolation

主な変更
1. 同一raceIdの結果取得を1本のPromiseへ統合。
2. 手動連打・auto重複は既存flightへjoin。
3. auto中の手動要求は新規fetchを増やさずmanualへ昇格。
4. 新規sync開始時の無条件AbortController.abort()を廃止。
5. requestIdを通常・復旧経路とWorkerレスポンスヘッダーで共有。
6. Safari TypeError名・メッセージ、visibility、online状態、段階時刻を保存。
7. 予想取得後のautoを800ms遅延し、autoに45秒cooldownを追加。
8. 別レースの古い応答はstaleとしてUIへ反映しない。
9. 保存済み結果、パーサー、競馬ロジック、D1構造は変更なし。

確認
- node --check app.js / worker.js / chass-latest.js / server.mjs: PASS
- npm test: 115/115 PASS
- npm run check: 115/115 PASS
