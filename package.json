{
  "name": "chass-keiba-lab",
  "version": "9.8.6",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test",
    "check": "node --check app.js && node --check chass-latest.js && node --check worker.js && node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
