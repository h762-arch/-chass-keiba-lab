export const GOVERNANCE_STAGES=Object.freeze(['DISCOVERY','SHADOW','APPROVED','PRODUCTION','MONITORING','ROLLED_BACK']);
export function sampleGate(count){const n=Number(count)||0;return n<20?'EXPLORATION':n<50?'SHADOW_CANDIDATE':'PROMOTION_REVIEW';}
export function canPromote(candidate={}){return candidate.sampleCount>=50&&candidate.walkForwardPass===true&&candidate.multiKpiImprovement===true&&candidate.materialRegression!==true&&candidate.baselineRetained===true&&['JRA','NAR'].includes(candidate.organization)&&!!candidate.theme&&candidate.explicitHumanApproval===true;}
export function transitionModel(model,to,{approval=false,reason='',at=new Date().toISOString()}={}){if(!GOVERNANCE_STAGES.includes(to))throw new Error('invalid_governance_stage');if(to==='PRODUCTION'&&(!approval||!canPromote({...model,explicitHumanApproval:true})))throw new Error('production_promotion_blocked');if(model.stage==='ROLLED_BACK'&&to==='PRODUCTION')throw new Error('rolled_back_version_immutable');return {...model,stage:to,updatedAt:at,events:[...(model.events||[]),{from:model.stage,to,at,reason}]};}
export function shadowEvaluation(candidate={}){try{return {ok:true,stage:'SHADOW',gate:sampleGate(candidate.sampleCount),productionChanged:false,candidate:{...candidate,adopted:false,officialModelChanged:false}}}catch(error){return {ok:false,stage:'SHADOW',productionChanged:false,error:error.message};}}
